node_modules
public/dist/*.map
package-lock.json

# Environment variables and sensitive configuration
.env
.env.local
.env.*.local
*.env

# Firebase configuration (keep your actual config local, don't commit it)
public/firebase-config.js
firebase-config.js

# Sensitive files
*.key
*.pem
secrets/
*.secret
